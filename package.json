{
  "name": "exercises-dataset-api",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "node scripts/build-api-data.mjs",
    "dev": "npm run build && vercel dev"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/node": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
