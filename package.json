{
  "name": "exercises-dataset-api",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node scripts/build-api-data.mjs",
    "dev": "npm run build && vercel dev"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.6.0"
  }
}
